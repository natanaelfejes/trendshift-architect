# Copy this file to .env (which is gitignored) and fill in your token.
# GitHub Settings -> Developer settings -> Fine-grained tokens ->
# Repository access: "Public Repositories (read-only)" -> Generate.
# Optional: without it, enrichment still works but is capped at 60 GitHub API requests/hour.
GITHUB_TOKEN=
